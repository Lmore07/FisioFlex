name: Build and Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Flutter
      uses: subosito/flutter-action@v2
      with:
        channel: beta

    - name: Install dependencies
      run: flutter pub get

    - name: Set up build environment
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential ninja-build libgtk-3-dev libwebkit2gtk-4.0-dev

    - name: Build Linux
      run: flutter build linux
